from ragbench import DatasetName
from ragbench.dataset_exploration.dataset_model import (
    DatasetAnswerScope,
    DatasetDocumentStructureFormat,
    DatasetDomain,
    DatasetQuestionContextDependency,
    DatasetRecord,
    DatasetRetrievalHops,
    DatasetTargetModality,
    StrictBaseModel,
)

_DATASETS: list[DatasetRecord] = [
    DatasetRecord(
        name=DatasetName.CLAP_NQ,
        domain=DatasetDomain.WIKIPEDIA,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="CLAP NQ is created from the subset of Natural Questions (NQ) that have a long answer but no short answer. NQ consists of ~380k examples. There are ~30k questions that are long answers without short answers excluding tables and lists. To increases the likelihood of longer answers we only explored ones that have more than 5 sentences in the passage. The subset that was annotated consists of ~12k examples. All examples where cohesion of non-consecutive sentences was required for the answer were annotated a second time. The final dataset is made up of all data that went through two rounds of annotation. (We provide the single round annotations as well - it is only training data) An equal amount of unanswerable questions have also been added from the original NQ train/dev sets.",
        url="https://huggingface.co/datasets/PrimeQA/clapnq",
        corpus_size=178890,
        train_size=1954,
        test_size=300,
    ),
    # DatasetRecord(
    #     name=DatasetName.AI_ARXIV,
    #     domain=DatasetDomain.SCIENTIFIC_PAPERS,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="""This dataset was derived from the ARAGOG benchmark (https://github.com/predlico/ARAGOG) of technical QA pairs over a corpus of machine learning papers from ArXiv.
    # This benchmark includes 107 QA pairs, without corresponding gold document labels.
    # As gold documents are not annotated in ARAGOG dataset, we added such labels where they could be found, obtaining 71 answerable QA pairs out of 107.
    # We use an enriched version of the benchmark. We, manually, added the ground-truth-contexts in the benchmark, and we limited the questions to the documents belonging to version https://huggingface.co/datasets/jamescalam/ai-arxiv2 (the version https://huggingface.co/datasets/jamescalam/ai-arxiv is not allowed by Legal)""",
    #     url="https://ibm.ent.box.com/file/1636760643755",
    #     corpus_size=2673,
    #     train_size=41,
    #     test_size=30,
    # ),
    DatasetRecord(
        name=DatasetName.MINI_WIKI,
        domain=DatasetDomain.WIKIPEDIA,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""This dataset, a subset generated by the RAG-Datasets team, supports research in question answering by providing questions and answers derived from Wikipedia articles, along with difficulty ratings assigned by both question writers and answerers. It includes files for questions from three student cohorts (S08, S09, and S10) and 690,000 words of cleaned Wikipedia text, facilitating exploration of question generation and answering tasks.""",
        url="https://huggingface.co/datasets/rag-datasets/rag-mini-wikipedia/",
        corpus_size=3200,
        train_size=663,
        test_size=255,
    ),
    # DatasetRecord(
    #     name=DatasetName.WIKIMULTIHOPQA,
    #     domain=DatasetDomain.WIKIPEDIA,
    #     retrieval_hops=DatasetRetrievalHops.MULTI_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="""2WikiMultihopQA: A Dataset for Comprehensive Evaluation of Reasoning Steps - https://github.com/Alab-NII/2wikimultihop  - A multi-hop question answering (QA) dataset aims to test reasoning and inference skills by requiring a model to read multiple paragraphs to answer a given question. However, current datasets do not provide a complete explanation for the reasoning process from the question to the answer. Further, previous studies revealed that many examples in existing multi-hop datasets do not require multi-hop reasoning to answer a question. In this study, we present a new multi-hop QA dataset, called 2WikiMultiHopQA, which uses structured and unstructured data. In our dataset, we introduce the evidence information containing a reasoning path for multi-hop questions. The evidence information has two benefits: (i) providing a comprehensive explanation for predictions and (ii) evaluating the reasoning skills of a model. We carefully design a pipeline and a set of templates when generating a question-answer pair that guarantees the multi-hop steps and the quality of the questions. We also exploit the structured format in Wikidata and use logical rules to create questions that are natural but still require multi-hop reasoning.""",
    #     url="https://huggingface.co/datasets/xanhho/2WikiMultihopQA",
    #     corpus_size=384857,
    #     train_size=126021,
    #     test_size=54009,
    # ),
    DatasetRecord(
        name=DatasetName.BIOASQ,
        domain=DatasetDomain.SCIENTIFIC_PAPERS,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="This dataset is a subset of a training dataset by the BioASQ Challenge, which is available here.\n"
        "\n"
        "It is derived from rag-datasets/rag-mini-bioasq.\n"
        "\n"
        "Modifications include:\n"
        "\n"
        "- filling in missing passages (some of them contained nan instead of actual text)\n"
        "- changing relevant_passage_ids' type from string to sequence of ints,\n"
        "- deduplicating the passages (removed 40 duplicates) and fixing the relevant_passage_ids in QAP triplets to point to the corrected, deduplicated passages' ids,\n"
        "- splitting QAP triplets into train and test splits.\n",
        url="https://huggingface.co/datasets/enelpol/rag-mini-bioasq",
        corpus_size=40181,
        train_size=4012,
        test_size=707,
    ),
    DatasetRecord(
        name=DatasetName.WATSONX_DOCS_QA,
        domain=DatasetDomain.TECHNICAL_DOCUMENTATION,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        url="https://huggingface.co/datasets/ibm-research/watsonxDocsQA",
        description="watsonxDocsQA is a new open-source dataset and benchmark contributed by IBM. The dataset is derived from enterprise product documentation and is designed specifically for end-to-end Retrieval-Augmented Generation (RAG) evaluation. The dataset consists of two components:\n"
        "\n"
        "Documents: A corpus of 1,144 text and markdown files generated by crawling enterprise documentation (main page - crawl March 2024).\n"
        "Benchmark: A set of 75 question-answer (QA) pairs with gold document labels and answers. The QA pairs are crafted as follows:\n"
        "25 questions: Human-generated by two subject matter experts.\n"
        "50 questions: Synthetically generated using the tiiuae/falcon-180b model, then manually filtered and reviewed for quality. The methodology is detailed in Yehudai et al. 2024.",
        corpus_size=2673,
        train_size=45,
        test_size=30,
    ),
    DatasetRecord(
        name=DatasetName.NARRATIVE_QA,
        domain=DatasetDomain.LITERATURE,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        url="https://huggingface.co/datasets/deepmind/narrativeqa",
        description="NarrativeQA is an English-lanaguage dataset of stories and corresponding questions designed to test reading comprehension, especially on long documents.",
        corpus_size=1457,
        train_size=32747,
        test_size=10557,
    ),
    DatasetRecord(
        name=DatasetName.HOTPOT_QA,
        domain=DatasetDomain.POLICIES,
        retrieval_hops=DatasetRetrievalHops.MULTI_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        subsets=[],
        url="https://huggingface.co/datasets/hotpotqa/hotpot_qa",
        description="Existing question answering (QA) datasets fail to train QA systems to perform complex reasoning and provide explanations for answers. We introduce HotpotQA, a new dataset with 113k Wikipedia-based question-answer pairs with four key features: (1) the questions require finding and reasoning over multiple supporting documents to answer; (2) the questions are diverse and not constrained to any pre-existing knowledge bases or knowledge schemas; (3) we provide sentence-level supporting facts required for reasoning, allowing QA systems to reason with strong supervision and explain the predictions; (4) we offer a new type of factoid comparison questions to test QA systems' ability to extract relevant facts and perform necessary comparison. We show that HotpotQA is challenging for the latest QA systems, and the supporting facts enable models to improve performance and make explainable predictions. ",
        corpus_size=482021,
        train_size=63313,
        test_size=27134,
    ),
    DatasetRecord(
        name=DatasetName.REAL_MM_FIN_REPORT,
        domain=DatasetDomain.FINANCIAL,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[
            DatasetTargetModality.TEXT,
            DatasetTargetModality.TABLE,
            DatasetTargetModality.IMAGE,
        ],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="We introduced REAL-MM-RAG-Bench, a real-world multi-modal retrieval benchmark designed to evaluate retrieval models in reliable, challenging, and realistic settings. The benchmark was constructed using an automated pipeline, where queries were generated by a vision-language model (VLM), filtered by a large language model (LLM), and rephrased by an LLM to ensure high-quality retrieval evaluation. To simulate real-world retrieval challenges, we introduce multi-level query rephrasing, modifying queries at three distinct levels—from minor wording adjustments to significant structural changes—ensuring models are tested on their true semantic understanding rather than simple keyword matching.\n"
        "REAL-MM-RAG_FinReport\n"
        "\n"
        "Content: 19 financial reports from 2005–2023.\n"
        "Composition: Includes both textual data and structured tables."
        "Purpose: Designed to test model performance on table-heavy financial data retrieval.",
        url="https://huggingface.co/datasets/ibm-research/REAL-MM-RAG_FinReport",
        corpus_size=2687,
        train_size=612,
        test_size=241,
    ),
    DatasetRecord(
        name=DatasetName.REAL_MM_TECH_REPORT,
        domain=DatasetDomain.TECHNICAL_DOCUMENTATION,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[
            DatasetTargetModality.TEXT,
            DatasetTargetModality.TABLE,
            DatasetTargetModality.IMAGE,
        ],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="We introduced REAL-MM-RAG-Bench, a real-world multi-modal retrieval benchmark designed to evaluate retrieval models in reliable, challenging, and realistic settings. The benchmark was constructed using an automated pipeline, where queries were generated by a vision-language model (VLM), filtered by a large language model (LLM), and rephrased by an LLM to ensure high-quality retrieval evaluation. To simulate real-world retrieval challenges, we introduce multi-level query rephrasing, modifying queries at three distinct levels—from minor wording adjustments to significant structural changes—ensuring models are tested on their true semantic understanding rather than simple keyword matching.\n"
        "REAL-MM-RAG_TechReport\n"
        "\n"
        "Content: 17 technical documents on IBM FlashSystem.\n"
        "Composition: Text-heavy with visual elements and structured tables."
        "Purpose:  Assesses model performance in retrieving structured technical content.",
        url="https://huggingface.co/datasets/ibm-research/REAL-MM-RAG_TechReport",
        corpus_size=1658,
        train_size=913,
        test_size=381,
    ),
    DatasetRecord(
        name=DatasetName.KRAMABENCH,
        domain=DatasetDomain.DATA_SCIENCE,
        retrieval_hops=DatasetRetrievalHops.MULTI_HOP,
        answer_scope=DatasetAnswerScope.CORPUS,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""KramaBench is a benchmark designed to evaluate AI systems on end-to-end data science pipeline tasks grounded in
        real-world data lakes. It includes 104 manually curated challenges drawn from 24 distinct data sources, comprising over 1700
        raw data files across 6 domains. Each task specifies a natural language objective requiring complex multi-step reasoning, data
        discovery, wrangling, cleaning, transformation, analysis, and insight extraction. Ground-truth reference pipelines and code
        solutions are provided to support evaluation of both final results and intermediate steps. AI systems are scored primarily on
        their ability to generate fully correct end-to-end pipelines, but auxiliary metrics assess pipeline design and individual sub-task
        implementation performance. The benchmark was introduced in a 2025 arXiv paper and has an associated GitHub repository for
        code and data. This benchmark goes beyond simple QA by demanding orchestration of data ingestion and processing workflows.""",
        url="https://huggingface.co/datasets/eugenie-y/KramaBench",
        corpus_size=104,
        train_size=0,
        test_size=104,
    ),
    DatasetRecord(
        name=DatasetName.DA_CODE,
        domain=DatasetDomain.DATA_SCIENCE,
        retrieval_hops=DatasetRetrievalHops.MULTI_HOP,
        answer_scope=DatasetAnswerScope.ARTIFACT,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[
            DatasetTargetModality.TEXT,
            DatasetTargetModality.TABLE,
            DatasetTargetModality.CODE,
            DatasetTargetModality.IMAGE,
        ],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""DA-Code is a benchmark for evaluating AI systems on end-to-end data science and analytics workflows through
        artifact-based evaluation. The benchmark consists of hundreds of real-world inspired tasks spanning data wrangling, statistical
        analysis, machine learning, data mining, and visualization. Each task provides a natural language instruction describing a
        concrete analytical objective, and systems are evaluated based on their ability to generate the correct output artifacts
        (e.g., CSV files, JSON outputs, NumPy arrays, images, databases) rather than textual answers.

        For each task, DA-Code supplies explicit evaluation configurations and gold reference artifacts that define how model outputs
        should be matched and validated. This enables deterministic, offline evaluation of full workflows, including intermediate
        computation steps, file formats, schemas, and numerical correctness. The benchmark emphasizes practical data-science
        competence, robustness to real data irregularities, and faithful execution of multi-step analytical pipelines, going beyond
        traditional question-answering benchmarks.""",
        url="https://github.com/yiyihum/da-code",
        corpus_size=2401,
        train_size=0,
        test_size=500,
    ),
    # DatasetRecord(
    #     name=DatasetName.ARROW_PUBLIC,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="""This dataset is comprised of SEC filings of Airline companies, and questions along with short answers on these reports. Most of the questions address tabular data.""",
    #     corpus_size=30,
    #     train_size=29,
    #     test_size=21,
    #     is_private_dataset=False,
    # ),
    DatasetRecord(
        name=DatasetName.OFFICEQA,
        domain=DatasetDomain.FINANCIAL,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TABLE, DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""OfficeQA is a benchmark by Databricks, built for evaluating model / agent performance on end to end Grounded Reasoning tasks.

        OfficeQA evaluates how well AI systems can reason over real-world documents to answer complex questions. The benchmark uses historical U.S. Treasury Bulletin PDFs (1939-2025), which contain dense financial tables, charts, and text data.""",
        corpus_size=285,
        train_size=172,
        test_size=74,
        is_private_dataset=False,
    ),
    DatasetRecord(
        name=DatasetName.DABSTEP,
        domain=DatasetDomain.FINANCIAL,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TABLE, DatasetTargetModality.TEXT],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""We introduce DABstep, a novel benchmark for evaluating AI agents on realistic multi-step data analysis tasks. DABstep comprises over 450 real-world challenges derived from a financial analytics platform, requiring models to combine code-based data processing with contextual reasoning over heterogeneous documentation. Each task demands an iterative, multi-step problem-solving approach, testing capabilities in data manipulation, cross-referencing multiple sources, and precise result reporting. The benchmark provides a factoid-style answer format with automatic correctness checks for objective scoring at scale.""",
        corpus_size=30,
        train_size=29,
        test_size=21,
        is_private_dataset=False,
    ),
    # DatasetRecord(
    #     name=DatasetName.SYNTHETIC_CLIENT_WINS,
    #     domain=DatasetDomain.SALES,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.CORPUS,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT],
    #     document_structure_format=DatasetDocumentStructureFormat.STRUCTURED,
    #     subsets=[
    #         DatasetName.SYNTHETIC_CLIENT_WINS_AGG_WITH_2_STRUCT_FILTERS,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_AGG_WITH_2_STRUCT_FILTERS_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_AGG_WITH_FIRST_STRUCT_THEN_SEMANTIC_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_SEMANTIC_FILTER,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_SEMANTIC_FILTER_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_STRUCT_AND_SEMANTIC_FILTER,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_STRUCT_AND_SEMANTIC_FILTER_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_STRUCT_FILTER,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_STRUCT_FILTER_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_AGG_WITH_STRUCT_GROUPBY_EXACT,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_LIST_WITH_2_STRUCT_FILTERS,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_LIST_WITH_SEMANTIC_FILTER,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_LIST_WITH_STRUCT_AND_SEMANTIC_FILTER,
    #         DatasetName.SYNTHETIC_CLIENT_WINS_DATA_LIST_WITH_STRUCT_FILTER,
    #     ],
    #     description="A synthetic dataset containing information about MFC client wins, including details about the client, the industry, the geographic location, the deal size, and the products and services involved.",  # TODO - To complete
    #     corpus_size=119,
    #     train_size=1233,
    #     test_size=528,
    # ),
    # DatasetRecord(
    #     name=DatasetName.AIT_QA_CURATED_DOCUMENTS_MD,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Recent advances in transformers have enabled Table Question Answering (Table QA) systems to achieve high accuracy and SOTA results on open domain datasets like WikiTableQuestions and WikiSQL. Such transformers are frequently pre-trained on open-domain content such as Wikipedia, where they effectively encode questions and corresponding tables from Wikipedia as seen in Table QA dataset. However, web tables in Wikipedia are notably flat in their layout, with the first row as the sole column header. The layout lends to a relational view of tables where each row is a tuple. Whereas, tables in domain-specific business or scientific documents often have a much more complex layout, including hierarchical row and column headers, in addition to having specialized vocabulary terms from that domain. To address this problem, we introduce the domain-specific Table QA dataset AITQA (Airline Industry Table QA). The dataset consists of 515 questions authored by human annotators on 116 tables extracted from public U.S. SEC filings (SEC Filings publicly available at: https://www.sec.gov/edgar.shtml) of major airline companies for the fiscal years 2017-2019. We also provide annotations pertaining to the nature of questions, marking those that require hierarchical headers, domain-specific terminology, and paraphrased forms. Our zero-shot baseline evaluation of three transformer-based SOTA Table QA methods - TaPAS (end-to-end), TaBERT (semantic parsing-based), and RCI (row-column encoding-based) - clearly exposes the limitation of these methods in this practical setting, with the best accuracy at just 51.8% (RCI). We also present pragmatic table pre-processing steps used to pivot and project these complex tables into a layout suitable for the SOTA Table QA models\n\n"
    #     "This version is a curated version containing the following changes:\n"
    #     "1 - Documents: AIT_QA has only tables, AIT_QA_curated has SEC filing pages as documents. Split to pages. Format markdown.\n"
    #     "2 - Questions: All questions in AIT_QA_curated are decontextualized.\n"
    #     "3 - GT answers : Some answers were corrected for AIT_QA curated.\n"
    #     "4 - GT retrieval: In AIT_QA only a single table, in AIT_QA_curated can be multiple pages.\n",
    #     url="https://github.com/IBM/AITQA",
    #     corpus_size=1937,
    #     train_size=360,
    #     test_size=155,
    # ),
    # DatasetRecord(
    #     name=DatasetName.AIT_QA_CURATED_FULL_PDF,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Recent advances in transformers have enabled Table Question Answering (Table QA) systems to achieve high accuracy and SOTA results on open domain datasets like WikiTableQuestions and WikiSQL. Such transformers are frequently pre-trained on open-domain content such as Wikipedia, where they effectively encode questions and corresponding tables from Wikipedia as seen in Table QA dataset. However, web tables in Wikipedia are notably flat in their layout, with the first row as the sole column header. The layout lends to a relational view of tables where each row is a tuple. Whereas, tables in domain-specific business or scientific documents often have a much more complex layout, including hierarchical row and column headers, in addition to having specialized vocabulary terms from that domain. To address this problem, we introduce the domain-specific Table QA dataset AITQA (Airline Industry Table QA). The dataset consists of 515 questions authored by human annotators on 116 tables extracted from public U.S. SEC filings (SEC Filings publicly available at: https://www.sec.gov/edgar.shtml) of major airline companies for the fiscal years 2017-2019. We also provide annotations pertaining to the nature of questions, marking those that require hierarchical headers, domain-specific terminology, and paraphrased forms. Our zero-shot baseline evaluation of three transformer-based SOTA Table QA methods - TaPAS (end-to-end), TaBERT (semantic parsing-based), and RCI (row-column encoding-based) - clearly exposes the limitation of these methods in this practical setting, with the best accuracy at just 51.8% (RCI). We also present pragmatic table pre-processing steps used to pivot and project these complex tables into a layout suitable for the SOTA Table QA models\n\n"
    #     "This version is a curated version containing the following changes:\n"
    #     "1 - Questions: All questions in AIT_QA_curated are decontextualized.\n"
    #     "2 - GT answers : Some answers were corrected for AIT_QA curated.\n"
    #     "3 - GT retrieval: In AIT_QA only a single table, in AIT_QA_curated can be multiple pages.\n",
    #     url="https://github.com/IBM/AITQA",
    #     corpus_size=1937,
    #     train_size=360,
    #     test_size=155,
    # ),
    # DatasetRecord(
    #     name=DatasetName.AIT_QA_CURATED_FULL_MD,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Recent advances in transformers have enabled Table Question Answering (Table QA) systems to achieve high accuracy and SOTA results on open domain datasets like WikiTableQuestions and WikiSQL. Such transformers are frequently pre-trained on open-domain content such as Wikipedia, where they effectively encode questions and corresponding tables from Wikipedia as seen in Table QA dataset. However, web tables in Wikipedia are notably flat in their layout, with the first row as the sole column header. The layout lends to a relational view of tables where each row is a tuple. Whereas, tables in domain-specific business or scientific documents often have a much more complex layout, including hierarchical row and column headers, in addition to having specialized vocabulary terms from that domain. To address this problem, we introduce the domain-specific Table QA dataset AITQA (Airline Industry Table QA). The dataset consists of 515 questions authored by human annotators on 116 tables extracted from public U.S. SEC filings (SEC Filings publicly available at: https://www.sec.gov/edgar.shtml) of major airline companies for the fiscal years 2017-2019. We also provide annotations pertaining to the nature of questions, marking those that require hierarchical headers, domain-specific terminology, and paraphrased forms. Our zero-shot baseline evaluation of three transformer-based SOTA Table QA methods - TaPAS (end-to-end), TaBERT (semantic parsing-based), and RCI (row-column encoding-based) - clearly exposes the limitation of these methods in this practical setting, with the best accuracy at just 51.8% (RCI). We also present pragmatic table pre-processing steps used to pivot and project these complex tables into a layout suitable for the SOTA Table QA models\n\n"
    #     "This version is a curated version containing the following changes:\n"
    #     "1 - Format markdown\n"
    #     "2 - Questions: All questions in AIT_QA_curated are decontextualized.\n"
    #     "3 - GT answers : Some answers were corrected for AIT_QA curated.\n"
    #     "4 - GT retrieval: In AIT_QA only a single table, in AIT_QA_curated can be multiple pages.\n",
    #     url="https://github.com/IBM/AITQA",
    #     corpus_size=1937,
    #     train_size=360,
    #     test_size=155,
    # ),
    # DatasetRecord(
    #     name=DatasetName.AIT_QA_CURATED_FULL_HTML,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Recent advances in transformers have enabled Table Question Answering (Table QA) systems to achieve high accuracy and SOTA results on open domain datasets like WikiTableQuestions and WikiSQL. Such transformers are frequently pre-trained on open-domain content such as Wikipedia, where they effectively encode questions and corresponding tables from Wikipedia as seen in Table QA dataset. However, web tables in Wikipedia are notably flat in their layout, with the first row as the sole column header. The layout lends to a relational view of tables where each row is a tuple. Whereas, tables in domain-specific business or scientific documents often have a much more complex layout, including hierarchical row and column headers, in addition to having specialized vocabulary terms from that domain. To address this problem, we introduce the domain-specific Table QA dataset AITQA (Airline Industry Table QA). The dataset consists of 515 questions authored by human annotators on 116 tables extracted from public U.S. SEC filings (SEC Filings publicly available at: https://www.sec.gov/edgar.shtml) of major airline companies for the fiscal years 2017-2019. We also provide annotations pertaining to the nature of questions, marking those that require hierarchical headers, domain-specific terminology, and paraphrased forms. Our zero-shot baseline evaluation of three transformer-based SOTA Table QA methods - TaPAS (end-to-end), TaBERT (semantic parsing-based), and RCI (row-column encoding-based) - clearly exposes the limitation of these methods in this practical setting, with the best accuracy at just 51.8% (RCI). We also present pragmatic table pre-processing steps used to pivot and project these complex tables into a layout suitable for the SOTA Table QA models\n\n"
    #     "This version is a curated version containing the following changes:\n"
    #     "1 - Format html\n"
    #     "2 - Questions: All questions in AIT_QA_curated are decontextualized.\n"
    #     "3 - GT answers : Some answers were corrected for AIT_QA curated.\n"
    #     "4 - GT retrieval: In AIT_QA only a single table, in AIT_QA_curated can be multiple pages.\n",
    #     url="https://github.com/IBM/AITQA",
    #     corpus_size=1937,
    #     train_size=360,
    #     test_size=155,
    # ),
    # DatasetRecord(
    #     name=DatasetName.AIT_QA_HTML_CURATED,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Recent advances in transformers have enabled Table Question Answering (Table QA) systems to achieve high accuracy and SOTA results on open domain datasets like WikiTableQuestions and WikiSQL. Such transformers are frequently pre-trained on open-domain content such as Wikipedia, where they effectively encode questions and corresponding tables from Wikipedia as seen in Table QA dataset. However, web tables in Wikipedia are notably flat in their layout, with the first row as the sole column header. The layout lends to a relational view of tables where each row is a tuple. Whereas, tables in domain-specific business or scientific documents often have a much more complex layout, including hierarchical row and column headers, in addition to having specialized vocabulary terms from that domain. To address this problem, we introduce the domain-specific Table QA dataset AITQA (Airline Industry Table QA). The dataset consists of 515 questions authored by human annotators on 116 tables extracted from public U.S. SEC filings (SEC Filings publicly available at: https://www.sec.gov/edgar.shtml) of major airline companies for the fiscal years 2017-2019. We also provide annotations pertaining to the nature of questions, marking those that require hierarchical headers, domain-specific terminology, and paraphrased forms. Our zero-shot baseline evaluation of three transformer-based SOTA Table QA methods - TaPAS (end-to-end), TaBERT (semantic parsing-based), and RCI (row-column encoding-based) - clearly exposes the limitation of these methods in this practical setting, with the best accuracy at just 51.8% (RCI). We also present pragmatic table pre-processing steps used to pivot and project these complex tables into a layout suitable for the SOTA Table QA models\n\n"
    #     "This version is a curated version containing the following changes:"
    #     "1 - Documents: AIT_QA has only tables, AIT_QA_curated has SEC filing pages as documents. In their original HTML format.\n"
    #     "2 - Questions: All questions in AIT_QA_curated are decontextualized.\n"
    #     "3 - GT answers : Some answers were corrected for AIT_QA curated.\n"
    #     "4 - GT retrieval: In AIT_QA only a single table, in AIT_QA_curated can be multiple pages.\n",
    #     url="https://github.com/IBM/AITQA",
    #     corpus_size=15,
    #     train_size=360,
    #     test_size=155,
    # ),
    # DatasetRecord(
    #     name=DatasetName.MULTIHIERTT_CURATED,
    #     domain=DatasetDomain.FINANCIAL,
    #     retrieval_hops=DatasetRetrievalHops.MULTI_HOP,
    #     answer_scope=DatasetAnswerScope.PASSAGE,
    #     question_context_dependency=DatasetQuestionContextDependency.UNCONTEXTUALIZED,
    #     targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
    #     document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
    #     description="Numerical reasoning over hybrid data containing both textual and tabular content (e.g., financial reports) has recently attracted much attention in the NLP community. However, existing question answering (QA) benchmarks over hybrid data only include a single flat table in each document and thus lack examples of multi-step numerical reasoning across multiple hierarchical tables. To facilitate data analytical progress, we construct a new large-scale benchmark, MultiHiertt, with QA pairs over Multi Hierarchical Tabular and Textual data. MultiHiertt is built from a wealth of financial reports and has the following unique characteristics: 1) each document contain multiple tables and longer unstructured texts; 2) most of tables contained are hierarchical; 3) the reasoning process required for each question is more complex and challenging than existing benchmarks; and 4) fine-grained annotations of reasoning processes and supporting facts are provided to reveal complex numerical reasoning. We further introduce a novel QA model termed MT2Net, which first applies facts retrieving to extract relevant supporting facts from both tables and text and then uses a reasoning module to perform symbolic reasoning over retrieved facts. We conduct comprehensive experiments on various baselines. The experimental results show that MultiHiertt presents a strong challenge for existing baselines whose results lag far behind the performance of human experts. The dataset and code are publicly available at https://github.com/psunlpgroup/MultiHiertt.",
    #     url="https://huggingface.co/datasets/yilunzhao/MultiHiertt",
    #     corpus_size=9902,
    #     train_size=624,
    #     test_size=305,
    # ),
    DatasetRecord(
        name=DatasetName.SECQUE,
        domain=DatasetDomain.FINANCIAL,
        retrieval_hops=DatasetRetrievalHops.SINGLE_HOP,
        answer_scope=DatasetAnswerScope.PASSAGE,
        question_context_dependency=DatasetQuestionContextDependency.CONTEXTUALIZED,
        targeted_modalities=[DatasetTargetModality.TEXT, DatasetTargetModality.TABLE],
        document_structure_format=DatasetDocumentStructureFormat.UNSTRUCTURED,
        description="""SECQUE is a comprehensive benchmark for evaluating large language models (LLMs) in financial analysis tasks.
        SECQUE comprises 565 expert-written questions covering SEC filings analysis across four key categories:

        - comparison analysis"
        - ratio calculation"
        - risk assessment"
        - financial insight generation.""",
        url="https://huggingface.co/datasets/nogabenyoash/SecQue",
        corpus_size=212,
        train_size=396,
        test_size=169,
    ),
]


class DatasetRegistry(StrictBaseModel):
    _datasets: list[DatasetRecord] = _DATASETS

    def filter(
        self,
        *,
        domains: list[DatasetDomain] | None = None,
        retrieval_hops: list[DatasetRetrievalHops] | None = None,
        answer_scopes: list[DatasetAnswerScope] | None = None,
        context_dependencies: list[DatasetQuestionContextDependency] | None = None,
        target_modalities: list[DatasetTargetModality] | None = None,
        document_structure_format: DatasetDocumentStructureFormat | None = None,
        include_private_datasets: bool = False,
    ) -> list[DatasetRecord]:
        results: list[DatasetRecord] = []

        for record in self._datasets:
            # NEW: Skip private datasets unless explicitly allowed
            if not include_private_datasets and record.is_private_dataset:
                continue

            if domains is not None and record.domain not in domains:
                continue

            if (
                retrieval_hops is not None
                and record.retrieval_hops not in retrieval_hops
            ):
                continue

            if answer_scopes is not None and record.answer_scope not in answer_scopes:
                continue

            if (
                context_dependencies is not None
                and record.question_context_dependency not in context_dependencies
            ):
                continue

            if target_modalities is not None:
                if not any(
                    modality in target_modalities
                    for modality in record.targeted_modalities
                ):
                    continue

            if (
                document_structure_format is not None
                and record.document_structure_format not in document_structure_format
            ):
                continue

            results.append(record)

        return results

    def list(self) -> list[DatasetRecord]:
        return self._datasets
